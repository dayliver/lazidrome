# 코드베이스 감사 (2026-07-09)

효율(백엔드/프론트엔드)과 미학·통일감 관점에서 전체 코드베이스를 분석한 결과.
**상태:** ✅ 완료 · 🔧 진행 중 · ⬜ 미착수

**2026-07-09 착수 (1~4):** B1·B2·B3·B4, D1~D3·D5, F3·F4 반영. **5번:** B5·B6(부분), F1·F2·F9, D6. 상세는 [CHANGELOG-AUDIT-2026-07.md](./CHANGELOG-AUDIT-2026-07.md).

---

## 1. 백엔드 효율

### 높음 (HIGH)

| # | 항목 | 위치 | 내용 | 상태 |
|---|------|------|------|------|
| B1 | 통계 play_history 로드 | `statsRepository.js` | 기간 필터 SQL + `/api/stats/top`에서 `getStatsTopPayload` 1회 로드로 tracks/albums/artists/totals 통합 | ✅ |
| B2 | 인덱스 | `schema.sql`, `db.js` `ensurePerformanceIndexes()` | artists/albums name, track_metadata file_id, play_history(track_id, played_at), album_tracks, playlist_tracks, track_artists | ✅ |
| B3 | 스캐너 변경 감지 | `scanner.js`, `track_filedata.mtime_ms` | `(path, size, mtime_ms)` 일치 시 해싱·파싱 스킵 | ✅ |
| B4 | 업로드·스캔 중복 | `scanner.js`, `lib/recentIngestPaths.js` | 업로드는 파일만 기록(DB는 스캐너 단일 경로). 스캔 완료·mtime 스킵 시 45s TTL로 chokidar 재진입 방지 | ✅ |
| B5 | 목록 API 무페이지네이션 | `albums.get.js`, `artists.get.js`, `pageQuery.js` | albums/artists `{ items, total, offset, limit, hasMore }` + legacy 배열 호환 | ✅ |
| B6 | 전 아티스트 top-tracks | `artistRepository.findTopTracksForArtistIds` | artists 목록 응답 시 페이지 ID만 스코프 | 🔧 |
| B7 | 홈 hot path DELETE | `pageVisitsRepository.findFrequentVisits` | prune를 타이머/배치로, NOT EXISTS | ⬜ |

### 중간·낮음

B8~B25: 홈 4중 쿼리, N+1 앨범 매칭, 이미지/스트림 Cache-Control, 스마트 믹스 RANDOM(), 태그 json_each 등 — ⬜ (우선순위 5 이후)

---

## 2. 프론트엔드 효율

### 높음 (HIGH)

| # | 항목 | 위치 | 내용 | 상태 |
|---|------|------|------|------|
| F1 | 부팅 시 전체 카탈로그 | `library.js`, `libraryCache.js`, `catalogApi.js` | revision-only boot + 페이지/검색 API | ✅ |
| F2 | 가상 스크롤 부재 | `VirtualScrollGrid`, `QueueList`, 트랙 테이블 | 그리드·큐·데스크톱 테이블 `@vueuse/core` useVirtualList | ✅ |
| F3 | 커버 서명 전량 선요청 | `AlbumsView`, `SafeImage.vue` | 앨범/아티스트 그리드: IntersectionObserver + `signType`/`signId`. AlbumsView prefetch 제거 | ✅ |
| F4 | 전역 mediaCacheVersion | `stores/auth.js` | `resourceVersions` Map으로 리소스별 반응형 버전 | ✅ |
| F5~F10 | 상세 필터, deep watch, 번들, i18n | 각 스토어·라우터 | F9: Home/FullPlayer/MetadataEdit lazy import ✅, 나머지 ⬜ |

### 잘 되어 있는 것

트랙 페이지 API 페이지네이션, hls.js 동적 import, 서명 배치·40ms 디바운스, WebSocket 재생 동기화.

---

## 3. 미학·통일감

| # | 테마 | 내용 | 상태 |
|---|------|------|------|
| D1 | 하트/별점 | `FavoriteButton`, `StarRating` — 플레이어·테이블·필터·컨텍스트 메뉴 | ✅ |
| D2 | 로딩 | `LoadingSpinner` — 주요 list/detail 뷰 | ✅ |
| D3 | 빈 상태 | `EmptyState` — 앨범·트랙·아티스트·히스토리 등 | ✅ |
| D4 | 색상 토큰 | `--favorite`, `--rating` in `style.css` | ✅ (일부 메타/플레이리스트 purple·green 잔존) |
| D5 | 라이트 모드 | 플레이어 `border-border`, Slider 썸 `bg-background`, QueueList hover `bg-muted` | ✅ |
| D6~D12 | radius, 타이포, 간격, 드롭다운, a11y, 모션 | D6: `rounded-xl` 카드·다이얼로그, [DESIGN-TOKENS.md](./DESIGN-TOKENS.md) | 🔧 (D6 ✅, D7~D12 ⬜) |

---

## 착수 순서 (권장)

1. ~~B2 + B1~~ ✅
2. ~~B3 + B4~~ ✅
3. ~~D1~D3 + D5~~ ✅ (D4 토큰 기초)
4. ~~F3 + F4~~ ✅
5. ~~F2 가상 스크롤, B5/F1 페이지네이션, F9 번들 분리, D6 radius 정규화~~ ✅
6. **다음:** B7 visit prune, B12~13 HTTP 캐시, B6 태그 상세 top-tracks, D7~D11

---

## 관련 파일 (1~4 구현)

| 영역 | 파일 |
|------|------|
| 인덱스·mtime | `backend/database/schema.sql`, `backend/src/db.js` |
| 통계 SQL | `backend/src/repositories/statsRepository.js` |
| 스캔 dedup | `backend/src/services/scanner.js`, `backend/src/lib/recentIngestPaths.js` |
| 커버 lazy sign | `frontend/src/components/shared/SafeImage.vue`, `AlbumGrid.vue`, `ArtistGrid.vue` |
| auth 서명 | `frontend/src/stores/auth.js` |
| UI 공용 | `FavoriteButton`, `StarRating`, `LoadingSpinner`, `EmptyState` |
| 테마 | `frontend/src/style.css` |
